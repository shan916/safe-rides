'use strict';

/**
 * @ngdoc function
 * @name safeRidesWebApp.controller:ResetpasswordrequestCtrl
 * @description
 * # ResetpasswordrequestCtrl
 * Controller of the safeRidesWebApp
 */
angular.module('safeRidesWebApp')
    .controller('ResetpasswordrequestCtrl', function () {
        this.awesomeThings = [
            'HTML5 Boilerplate',
            'AngularJS',
            'Karma'
        ];
    });
